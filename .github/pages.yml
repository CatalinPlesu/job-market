# GitHub Pages Configuration
# This file is used by GitHub Pages to configure the site

# Use the pages directory as the source
# Note: You may need to configure this in GitHub repository settings
# Settings -> Pages -> Source -> Deploy from a branch -> Select branch and /pages folder

# The site will be available at:
# https://{username}.github.io/{repository-name}/
